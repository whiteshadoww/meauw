<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            android:id="@+id/nav_main_center_fragment"
            app:startDestination="@id/swipeFragment">

    <fragment android:id="@+id/swipeFragment" android:name="me.snowshadow.meauw.frags.SwipeFragment"
              android:label="fragment_swipe" tools:layout="@layout/fragment_swipe">
        <action android:id="@+id/action_swipeFragment_to_topPicksFragment"
                app:destination="@id/topPicksFragment"/>
    </fragment>
    <fragment android:id="@+id/topPicksFragment"
              android:name="me.snowshadow.meauw.frags.TopPicksFragment"
              android:label="TopPicksFragment"/>

</navigation>