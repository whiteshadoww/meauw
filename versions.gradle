ext {
    def build = [:]

    build.compile_sdk = 28
    build.min_sdk = 19
    build.target_sdk = build.compile_sdk
    build.app_id = "me.snowshadow.meauw"

    def plugin = [:]

    plugin.kotlin = '1.3.21'
    plugin.gradle = "3.3.2"

    def versions = [:]

    versions.room = "2.1.0"
    versions.lifecycle = "2.0.0"
    versions.navigation = "2.1.0-alpha06"

    versions.androidx = "1.0.0-beta01"
    versions.core_ktx = "1.2.0-alpha02"
    versions.constraintlayout = "1.1.3"
    versions.retrofit = "2.1.0"
    versions.dagger = "2.11"
    versions.jre = "1.2.71"
    versions.plugin = plugin

    ext.conf_build = build
    ext.versions = versions
}